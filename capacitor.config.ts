import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'promptpay-app-upgrade',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
